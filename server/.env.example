# Server Configuration
PORT=5000
NODE_ENV=development

# Database (Optional - falls back to built-in in-memory store if omitted)
MONGODB_URI=mongodb://127.0.0.1:27017/mospi_skill_intelligence

# JWT Authentication Secret
JWT_SECRET=mospi_sih_2026_super_secret_jwt_key_prod

# AI MCQ Generator API Keys (Optional - falls back to intelligent built-in generator if omitted)
ANTHROPIC_API_KEY=your_anthropic_claude_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
